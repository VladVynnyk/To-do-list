import React from 'react';

const Form = () => {
    return (
        <div>
            <input type="text"/>
            <input type="text"/>
            <button>Add task</button>
        </div>
    );
};

export default Form;